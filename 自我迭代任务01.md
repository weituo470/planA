# 自我迭代任务 01：提升原子化任务表评分到 85+

## 用户原始需求（复述）

在 planA 项目里，按页面流程从“原始需求”一步步生成到最终的 `tasks_atomic`，并用模型评分；在不靠同一版代码反复重试刷分的前提下，通过迭代本项目的代码/提示词，把原子化任务表评分提升到 **85+**（历史最高 76）。

同时需要：

- 生成到 `docs/` 的流程报告文件名从“原始需求”截取片段用于命名，便于回溯。
- 评分报告生成后提供“迭代”按钮：收集各模型对原子化任务表单的评价，并提供用户修改意见输入框；把这些反馈连同“原子化之前的输入”（不是已原子化的任务表）一起发给当前选择模型重新生成。
- 修复：评分时拿到的报告不是本次生成而是更早之前的（runId/文档绑定错误）。
- 修复：提示词明确禁止 `TBD`，但原子化任务表里仍出现 `TBD` 的问题。
- 页面文案/交互：把“对全部模型评分”改为“用模型评分”；等待过程中的剩余时间显示在“进行中”文字后。
- 前后端可稳定重启，`http://localhost:3030/health` 可正常响应，避免 `ERR_CONNECTION_REFUSED / Failed to fetch`。
- Git 策略：首次达到 **≥80** 才允许提交；之后每提升 **≥5** 分提交一次；最终推送到 GitHub。

## 目标

在当前项目（planA：Bridge + Dashboard）内，通过 **MCP 浏览器自动化**按 UI 流程从“原始需求”开始完整跑通：

1. 生成 `requirements`
2. 生成 `design`
3. 生成 `tasks`
4. 生成 `tasks_atomic`（原子化任务表）
5. 使用“用模型评分”对 `tasks_atomic` 评分

并通过迭代本项目的 **代码/提示词**，让同一套流程的评分达到 **85 分以上**。

> 历史基线：用户曾拿到的最高分为 76。

## 约束与原则

- 迭代对象：本项目的代码/提示词（例如 Bridge 的 prompt 配置、生成/校验/后处理逻辑、UI 交互）。
- 不靠“同一版代码反复重试生成”刷分：每次提升都基于明确的代码/提示词调整后，再跑 1 次流程得到分数。
- 迭代输入必须基于“原子化之前的内容”（例如 `tasks.md` + 评分反馈 + 用户补充），不得把已生成的 `tasks_atomic.md` 当作迭代重写的唯一输入。
- 评分达到阈值时执行 Git 提交策略（见下节）。
- 所有代码变更在交付前必须做基础自测：
  - `GET http://localhost:3030/health` 返回 `{"ok":true}`
  - `dashboard` 构建：`npm run build`

## Git 提交规则

- 当首次把评分做到 **≥ 80**：做一次 `git commit`
- 之后每当“最佳评分”相对上一次提交对应的分数 **提升 ≥ 5 分**：再做一次 `git commit`
- 直到评分 **≥ 85** 达成
- 达成目标并确认无误后：`git push` 推送到 GitHub

## 工作流（由 MCP 执行）

### A. 基线复现（一次完整流程）

1. 打开 Dashboard 页面
2. 输入原始需求（用于稳定对比；必要时包含“静态站/无后端/无登录”等明确约束）
3. 点击生成需求 → 完成需求确认（含需求澄清问题）
4. 点击生成设计 → 完成技术栈确认（尽量选择：不需要后端/不需要数据库/无登录/静态托管）
5. 点击生成任务
6. 点击开始原子化，直到 `tasks_atomic` 完成
7. 点击“用模型评分”并等待评分完成
8. 记录分数、弱项与建议（作为下一轮迭代依据）
9. 若需要：点击“迭代”按钮，汇总各模型评价 + 用户修改意见，并基于“原子化之前的输入”触发重新生成

## 已完成的关键修复/迭代（当前代码状态）

- 修复评分/迭代绑定错误：评分不再误用更早报告，改为 run 级别 artifacts 快照（避免 spec 目录“最新文件”污染历史 run）。
- 原子化输入增强：从 `tasks.md` 解析每条任务的 `title/core/details/ac` 提供给 atomize，不再只喂标题。
- 更严格的原子化校验与后处理：禁止 `TBD/待定/[path]/占位符`；路径必须带扩展名；合并重复的 `docs/assumptions.md` 类型任务为单条“汇总待确认点”。
- UI：按钮文案改为“用模型评分”；原子化进度的 ETA 放在“进行中”后。
- 评分截断放宽：`tasks_atomic` 评分输入截断长度扩大到 60k，减少“内容不完整”扣分。
- 启动方式兼容 Windows：避免 `Start-Process npm` 触发 `%1 不是有效的 Win32 应用程序`，改用 `cmd.exe /c` 启动并将日志输出到 `.runlogs/`。

### A1. 最新基线结果（2026-01-04）

- Spec：`plana-1timeoutformatnetw-20260104010414`
- RunId：`20260104010414-b-hL7K`
- 流程报告：`docs/flow_为_planA_增加“原子化失败可诊断_+_一键_20260104010414-b-hL7K.md`
- 模型评分：
  - ChatGPT-5.2-Codex：**72/100**
  - Gemini：403（error）
  - Claude：502（error）

主要弱项（摘自 ChatGPT-5.2-Codex 评价 + 观察到的运行情况）：

1. 原子任务的“统一性/一致性”仍不足（命名、模块边界、失败原因归类、重试策略等表述不一致）。
2. 部分验收标准仍偏“静态检查”（仅 `rg`/文本检索），对 Windows 环境下可复现的行为验证不够具体。
3. 任务之间的依赖/顺序还有可优化空间（例如：先定义接口/契约与错误码，再做 UI/交互与测试）。
4. 多模型评分存在网关不可用（403/502）导致覆盖不足（属于环境问题，但会影响“对全部模型评分”的体验）。

下一轮（Iteration-1）改动方向（只改代码/提示词，不刷随机重试）：

- 原子化输出的“一致性约束”再加强（统一任务格式、命名、模块边界、错误与重试策略的表述模板）。
- AC 统一要求“可运行验证”（接口/命令/UI 操作）而不仅是搜索/静态检查，且能在 Windows 下复现。
- 迭代按钮链路确保：收集多模型评价 + 用户补充，并以“原子化前输入”为根，驱动重新生成（避免用已原子化产物误导）。

### B. 针对性迭代（只改代码/提示词，不靠重复生成）

按评分建议/弱项分类做调整，例如：

- 提示词：更严格禁止占位符、减少重复任务、强化 AC 的“可执行验证”，限制无关范围
- 生成逻辑：减少超时回退、提升输出稳定性（超时参数/重试策略/拆分策略）
- 后处理：全局去重、合并重复的 `docs/assumptions.md` 创建任务、确保顺序“定义先行”

每次调整后重复执行 **A**，得到新分数。

## 产出与验收

- `tasks_atomic.md` 中无 `TBD/待定/[path]/占位符`
- 评分达到 **≥ 85**
- Git 提交记录符合提交规则
- 最终推送到 GitHub
