# MVP 目标

## 目标定义
在本地 1 台机器上跑通「规范驱动 + 可观测 + 可交互」的最小闭环：
- CLI 执行可视化（实时日志、状态、Diff）
- 浏览器侧可发起审批与需求补丁
- DAG 任务图可更新并同步

## 用户正反馈指标
- 用户能在浏览器看到状态流转（Thinking/Executing/Reviewing）
- 用户能实时看到文件 Diff，不依赖终端滚屏
- 用户能在 Web 端点击审批并让系统回到可执行状态
- 用户能提交需求补丁并在事件流中确认

## 不做（MVP 外）
- 多用户协作与权限管理
- 远程部署与账号体系
- 全量指标分析与性能监控
