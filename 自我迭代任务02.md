# 自我迭代任务 02：企业官网原子化任务表评分冲刺到 90+（连续两次达标）

> 记录对象：planA（Bridge + Dashboard）在“开发一个企业官网”固定原始需求下的自我迭代与跑分结果。  
> 本文不包含任何密钥/凭据。

## 0) 原始需求（固定）

- 原始需求：**开发一个企业官网**
- 判定规则：**按“≥”**；**连续两次评分达标**才算通过；5 分一档推进与提交；达到 90 分后提交并推送。

## 1) 环境与执行方式

- 运行环境：Windows + PowerShell（`approval_policy=never`，允许网络访问）
- 评分方式：使用 Bridge 的 HTTP API 自动跑通全流程并触发模型评分（避免浏览器自动化链路不稳定导致流程中断）。
- Bridge 端口：
  - 健康检查：`http://localhost:3030/health`
  - 说明：为避免与本机其他版本冲突，本仓库默认端口固定为 `3030`
- SpecRoot：`D:\demo\workflow\specs`（运行时 spec 产物不落在仓库内）

## 2) 全流程（本轮跑分脚本化口径）

对每次跑分固定执行：

1. `POST /specs` 创建新 spec（prompt 固定为“开发一个企业官网”）
2. `GET /specs/:name/requirements` 生成需求
3. 写入并确认需求澄清问题（选项自动填充 + “其他”补充约束）
4. `GET /specs/:name/design` 生成设计
5. 写入并确认技术栈（固定选择：前端 `纯 HTML/CSS/JS`；后端/数据库/鉴权 `none`；部署 `static`）
6. `GET /specs/:name/tasks` 生成 tasks
7. `POST /specs/:name/tasks/atomize` 原子化生成 tasks_atomic（分段 batchSize=20/30）
8. `POST /specs/:name/reports/:runId/score` 触发评分并轮询完成

## 3) 关键问题与修复点（导致分数提升的主要原因）

### 3.1 原子化兜底任务越界（导致低分/回归）

- 现象：当 LLM 输出不合规时，兜底逻辑可能生成不在当前原始任务范围内的文件路径（例如落到 `dashboard/src/*`），被评分显著扣分。
- 修复：兜底生成原子任务时也强制遵守当前原始任务的 `pathPolicy`（只允许该原始任务 details/title 给出的路径集合 + assumptions 例外口）。
  - 关键位置：`bridge/src/index.js` 中 `coerceAtomicTasks(..., pathPolicy)`

### 3.2 评分/报告使用的 runId 绑定与“可复盘性”

- 使用 FlowRun（runId）作为 artifacts 快照与评分的绑定对象，避免“拿到更早报告/更早产物”导致评分漂移。
- 产出：每次原子化完成会生成对应 `docs/flow_开发一个企业官网_<runId>.md`（本地可读、便于复盘）。

### 3.3 规范一致性扣分：CSS class 与 i18n 映射的收敛

- CSS：将容易漂移的 class 约束归一（例如将 `cards-grid` 统一归一为 `grid`），减少“规范冲突/多套命名”扣分。
- i18n：补齐“最小映射范围”（哪些元素必须打 `data-i18n-key`），避免“词条存在但页面不标注/验收口径不一致”扣分。

## 4) 跑分记录（gpt-5.2-codex）

> 评分均来自 Bridge API 的 `/reports/:runId/score` 结果（按“≥”判定）。

| 序号 | specName | runId | 分数 | 报告文件（本地） |
|---:|---|---|---:|---|
| 1 | spec-20260105115237 | 20260105115314-uTjWbv | 88 | `docs/flow_开发一个企业官网_20260105115314-uTjWbv.md` |
| 2 | spec-20260105120838 | 20260105120950-nF4kAL | 84 | `docs/flow_开发一个企业官网_20260105120950-nF4kAL.md` |
| 3 | spec-20260105123516 | 20260105123600-cMNlCB | 88 | `docs/flow_开发一个企业官网_20260105123600-cMNlCB.md` |
| 4 | spec-20260105125510 | 20260105125558-nBX6zU | **92** | `docs/flow_开发一个企业官网_20260105125558-nBX6zU.md` |
| 5 | spec-20260105131220 | 20260105131304-_JSKgS | **92** | `docs/flow_开发一个企业官网_20260105131304-_JSKgS.md` |

结论：**连续两次评分 92、92（≥90）达标**。

## 5) Git 提交与推送（按档位推进）

- 关键提交（本轮新增/直接相关）：
  - `446c83c` `fix: constrain atomize fallback paths`
  - `e8b0194` `fix: align website task conventions`
- 推送结果：已推送到 `origin/master`（GitHub 仓库：`https://github.com/weituo470/planA`）
- 过程中处理了：
  - 远端 `master` 先行更新导致 push 拒绝（fetch/rebase 后再 push）
  - Git 在 rebase 时要求提交者身份：通过临时环境变量/参数完成 rebase 继续（未修改全局 git config）

## 6) 交付前基础自测（已通过）

- Bridge 健康检查：`GET http://localhost:3030/health` 返回 `{"ok":true}`
- Dashboard 构建：`npm --prefix dashboard run build` 成功

## 7) 上下文长度不足的处理方案（本轮经验）

- **单一事实源**：把不可变约束集中到 `docs/assumptions.md`（并在原子任务中只引用，不重复粘贴整段规则）。
- **差异驱动迭代**：迭代时只给“评分弱项 + 需要收敛的约束差异”，避免每轮把完整 tasks/tasks_atomic 全量塞回模型导致截断与漂移。
- **范围收敛**：对每个原始任务建立可输出路径白名单（`pathPolicy`），让模型与兜底逻辑都只能在小范围内生成，减少发散与重复。
