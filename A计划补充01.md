# A计划补充01（Project Plan A）架构增补与进化说明（v0.5）

## 1. 核心定位更新
A计划不仅是一个自动化编程工具，它是一个具备长效记忆、自我进化能力、且由人类指挥官高度可控的本地 AI 开发操作系统。它通过封装多 Agent 协同（CODEX + GLM/Claude）与本地工程化工具，解决大型复杂项目的长序列开发痛点。

## 2. 新增核心特性（New Features）

### 2.1 自我进化机制：AI 错题本（The Experience Store）
- 记录逻辑：自动捕获开发中的“弯路”，记录格式为：报错/困境上下文 -> 尝试方案 A（失败原因）-> 修复方案 B（成功要点）。
- 长效记忆：存储于本地向量库。当 Agent 再次遇到类似报错（如特定的环境配置错误）时，自动检索并注入之前的成功经验，避免同一坑位反复跌倒。

### 2.2 强化检查点与“一键本地验证”
- 巡检卡片（Inspection Card）：人类介入时，系统提供风险分级（红/黄/绿）与 Diff 摘要，明确告知人类“为什么需要你来看”。
- 本地一键测试：
  - 集成 node-pty：在浏览器中实现实时、带彩色的本地终端流。
  - 上下文感知测试：Agent 自动识别受改动影响的模块，优先运行相关单元测试，而非全量扫描。
  - 自愈测试：如果测试失败，Agent 自动读取本地报错，尝试自主修复后再请求人类复核。

### 2.3 Agent Skill 基础库（Skill Registry）
- 原子化能力：预置常用的复杂 Skill（如：UI-Gen 视觉比对、SQL-Optimizer 索引优化、Standard-Auth 模板注入）。
- 动态调用：Agent 根据任务需求，从库中挑选最合适的脚本工具执行，确保输出的代码符合行业最佳实践。

### 2.4 动态项目文档系统（Living Docs）
- 文档即代码：自动将 Kiro Spec 转化为交互式文档（如 VitePress 风格），在浏览器端实时预览。
- 双向联动：在文档中修改接口定义，可反向触发 Agent 的代码重构任务。

## 3. 上下文与意图持久化（Memory & Intent）
针对 AI 窗口 Token 上限和“失忆”问题的专项方案。

### 3.1 意图快照（The Mental Snapshot）
- 思维断点续传：在会话结束或切换时，Agent 强制输出一个 handoff.json，记录“我刚才在干嘛”、“我怀疑哪里有问题”、“下一步的直觉是什么”。
- 接力棒协议：新窗口启动时，主控程序自动读入快照，瞬间恢复 AI 的“思维惯性”，实现跨聊天窗口的无缝衔接。

### 3.2 意志锚点（Visual Anchor）
- 浏览器 DAG 图：以任务拓扑图作为“全局唯一事实来源”。即使后端 Agent 重启，浏览器端的任务进度、思考路径和代码 Diff 依然保持悬挂，作为 AI 的视觉记忆。

### 3.3 Windsurf 灵感集成（Flow & Cascade）
- 动作链条记录：借鉴 Windsurf，不只记录代码变动，更记录“动作链”（Action Chain）。AI 知道当前动作是为解决前一个动作产生的 Bug 服务的。
- 本地环境深度感知：利用本地 LSP（语言服务器）和文件索引，让 Agent 具备“上帝视角”，能够跨文件精准定位定义与引用。

## 4. 技术实施要点（Implementation）
- 本地优先：所有的代码执行、测试、错题库检索均在本地运行，确保隐私与响应速度。
- Git-Ops 闭环：每一个子任务自动创建分支，失败则回滚，成功则原子化 Commit。Git 历史作为物理层的“终极记忆”。
- 安全防护：
  - .gitignore 强制忽略 .env 和本地敏感记忆库。
  - 对 rm 等高危指令设置人类二次确认钩子。

## 5. 阶段性交付形态
- 一个本地 MCP Server：作为能力引擎，适配 Cursor/Claude/IDE。
- 一个浏览器指挥控制台：包含任务图、实时终端、Diff 预览、一键测试按钮，以及动态文档看板。
- 一套本地存储规范：.plan-a/ 目录下存储所有进化数据、Skill 脚本和思维快照。
- A计划座右铭：让 AI 拥有记忆，让开发拥有导航。

## 项目备注
- [说明]：此文档标志着 A计划 从“规范驱动”进化到了“智能流驱动（Flow-Driven）”。下一阶段将重点攻克 node-pty 与浏览器实时通讯以及本地向量库的语义索引两个核心技术点。
