# 任务提示词/执行文档优化（仅保留必要信息）

## 背景：当前两层信息

目前一次“自动开始任务”会涉及两层文本：

1) **终端里传给 AI 工具的 prompt（命令行参数）**  
   - 特点：会被压成单行（`\n` → 空格），并作为 `cmd.exe /c claude ... "<prompt>"` 的参数传入。  
   - 用途：告诉 AI “该做哪个任务、去看哪份任务文档、完成/失败时输出什么标记”。

2) **任务执行文档（runDoc，Markdown 文件）**  
   - 特点：内容较长，包含 spec 快照/任务信息/执行要求等。  
   - 用途：承载详细信息与验收口径，供 AI 工具在执行时打开阅读；也会在 Dashboard 的「任务文档」里展示给人看。

本文件聚焦：让 **prompt 与 runDoc 都只包含必要信息**，减少冗余与噪音，提升可执行性与稳定性。

---

## 主要问题（为什么需要“瘦身”）

1) **命令行长度与转义风险（Windows）**  
   prompt 作为 `cmd.exe` 的参数，过长会触发长度上限/转义异常，导致“看起来粘贴了但没跑/跑不起来”。

2) **可读性问题**  
   prompt 被压成单行后，多段指令、代码块格式都会失效；越长越难读、越容易漏关键信息（尤其是完成标记）。

3) **信息重复/无关上下文过多**  
   runDoc 里塞入大段 requirements/design/tasks 快照会：  
   - 增加阅读成本与 token 消耗  
   - 让 AI 更容易跑偏（抓住不相关段落做“宏观改造”）

4) **“给人看的任务文档”也需要更直观**  
   Dashboard 展示 runDoc 时，最需要的是：这任务要做什么、做到什么算完成、如何验证。

---

## 优化目标（只给必要内容）

1) prompt **极短且稳定**：不依赖换行，不依赖复杂格式，不触发长度/转义问题。  
2) runDoc **以“交付闭环”为中心**：一眼看懂目标/验收/范围/验证。  
3) **分层**：prompt 只放“指路+完成信号”；runDoc 放“怎么做/怎么验收”。  
4) **尽量避免大段快照**：只在“确实需要上下文”时内嵌，并严格截断。

---

## 建议的最小信息集

### A) 终端 prompt（命令行参数）最小集

建议只保留这些字段（按重要度排序）：

1) `TASK_ID`（必须）  
2) `runDocPathAbs`（强烈建议，作为唯一详情入口）  
3) `projectDir/cwd`（建议）  
4) 完成/失败标记（必须，且要“短、不可变、易复制”）

推荐的**一行 prompt 模板**（无换行依赖）：

> 执行任务 TASK_ID=task_3；按任务文档 <runDocPathAbs> 实现；建议目录 <projectDir>；完成输出 [[TASK_DONE task_3]]；失败输出 [[TASK_FAILED task_3]]

说明：
- 把“完成/失败输出什么”写成**短句**，避免“最后单独输出一行”等对换行敏感的表达。
- 只放 1 个任务的关键入口，避免把任务描述复制进 prompt（描述留给 runDoc）。

### B) runDoc（任务执行文档）最小集

建议 runDoc 固定为这些块，且每块尽量短：

1) **任务摘要（1 屏内）**
   - Task：`task_x` / Title
   - 目标（1 句）
   - 验收标准（≤ 5 条）
   - 修改范围（scope/file list，≤ 20 行）
   - 依赖/前置（dependencies，≤ 10 行）

2) **实现约束（只写“必须遵守的”）**
   - 不做超范围重构 / 不改无关模块
   - 不引入新依赖（除非任务明确要求）
   - 不提交敏感信息/不改环境文件（如适用）

3) **自测与验证（可复制的命令 + 期望结果）**
   - 只列与该任务相关的最小命令集（例如：`npm run build` / 单接口健康检查）
   - 写清楚“看到什么算通过”

4) **产出格式（给编排器解析/给人阅读）**
   - 输出：关键变更 / 验证方式 / 验证结果
   - 完成信号：`[[TASK_DONE task_x]]`（可重复写一遍，防止遗漏）

> 关键点：runDoc 要同时服务“AI 执行”与“人阅读”，所以“目标/验收/验证”优先级最高。

---

## 关于 spec 快照（建议策略）

默认策略：**不内嵌 requirements/design/tasks 全文快照**。改为：

- 只写 “相关的 3~10 行摘要/要点”
- 给出原始文件路径（供人定位）即可

仅当满足以下条件之一时才内嵌片段：
- 任务必须依赖某段具体文字/接口定义/字段约束，否则容易误解  
- 或任务是“需求梳理/文档生成”类，必须引用原文

并且内嵌内容必须：
- 只截断到“足够完成该任务”的范围（例如 ≤ 1500~3000 字）
- 明确标注“这是片段，不是全部”

---

## 可执行的下一步（不改代码的前提下）

短期：只靠 runDoc 内容调整，就能显著变“更短更好用”。  
中期：再考虑把 prompt 模板进一步缩短，并让 runDoc 输出更结构化（便于 UI 折叠/搜索）。

（如果后续要落地到实现层，主要改动点在：生成 runDoc 的函数与 task-prompt 的 prompt 模板。）

