# planA 项目优化建议报告（项目经理冒烟测试）

> 测试目标：以“从原始需求 → 生成 requirements/design/tasks → 原子化 → 一键下载 ZIP 交付”的主路径为核心，验证可用性与可诊断性，并给出可执行的优化建议（按优先级）。

## 1. 测试概览

- 测试时间：2026-01-02
- 测试环境：Windows · Chrome 143 · Bridge `http://localhost:3030` · Dashboard `http://127.0.0.1:3031/`
- 活跃模型：`gpt-5.2-codex`（`/llm/ping` 约 `5270ms`）
- 测试方式：浏览器端到端冒烟 + 关键接口自检（`/health`、`/specs`、`/prompts`、`/llm/ping`）

## 2. 主路径结果（结论先行）

- ✅ Bridge/Dashboard 均可启动，关键接口可用（见第 6 节）
- ✅ 可完整跑通：生成需求 → 需求确认并生成设计 → 技术栈确认并生成任务 → 分段原子化 → 一键下载 ZIP
- ✅ requirements/design/tasks 三个内容窗口支持 **流式填充**（不再在“加载中弹窗”里等待）
- ✅ 一键下载 ZIP 内 `使用说明.txt` 已明确：交付给 AI IDE 时 **优先按 `tasks_atomic.md`（原子化任务表单）逐条开发**
- ✅ 原子化全部完成后，服务端自动落盘 **完整流程报告** 到 `planA/docs/flow_*.md`（含：原始需求→原子化任务、模型/提示词、耗时与 token 等）

## 3. 关键发现（按优先级）

### P0（阻断/高风险）

- 无

### P1（显著影响体验/可维护性）

- 原子化过程中出现“原子化失败，已生成占位任务。”：目前只能看到“失败/占位”结果，缺少失败原因分类（网络/鉴权/解析/格式校验）、失败任务定位与一键重试能力
- 控制台无障碍告警：`A form field element should have an id or name attribute (count: 28)`，影响可访问性与自动化测试稳定性

### P2（体验优化/工程一致性）

- “实时输出”会直接渲染模型原始流（包含如 `<think>...</think>` 等内容），建议做输出净化/折叠，避免干扰用户阅读
- Spec 存放路径存在“仓库目录 vs 运行时目录”认知成本：当前 Spec 实际落在 `C:\\D\\codex\\workflow\\specs`（而非仓库根目录下），建议在 UI/文档中明确标注与可配置化

## 4. 优化建议（可执行）

| 优先级 | 建议 | 落地抓手 | 验收口径 |
|---|---|---|---|
| P1 | 原子化失败可诊断 + 可重试 | 在原子化日志中补充失败任务编号、失败原因（含分类码）、原始响应摘要；提供“重试失败任务/重试本段”按钮（保持幂等） | 失败时用户能在 10 秒内定位原因并一键重试；不会默默生成大量占位任务 |
| P1 | 补齐表单 `id/name` | 为关键输入补齐 `id/name`，并用 `<label htmlFor>` 绑定；必要时补充 `aria-label` | 控制台对应 a11y 告警清零（或显著下降并记录为技术债） |
| P2 | 流式输出净化/折叠 | 前端对 token 流做过滤：隐藏/折叠 `<think>...</think>`；对 JSON 片段可选择性高亮/折叠 | “实时输出”更可读，不影响理解任务/设计内容 |
| P2 | Spec 根目录可视化/可配置 | UI 显示 Spec Root，bridge 支持 env 配置覆盖（并在启动日志打印最终路径） | 新同学 5 分钟内理解 Spec 存放位置；迁移/备份更明确 |

## 5. 回归用复现步骤（端到端）

1. 启动 Bridge：在 `bridge/` 运行 `npm run dev`
2. 启动 Dashboard：在 `dashboard/` 运行 `npm run dev -- --host 127.0.0.1 --port 3031 --strictPort`
3. 打开 `http://127.0.0.1:3031/`：
   - 输入“原始需求”→ 点击“生成需求”
   - 在“生成中”弹窗观察 **实时输出**阶段变化/流式内容
   - 在“需求”完成必填确认 → 点击“生成设计”
   - 在“设计”完成必填确认 → 点击“确认技术栈并生成任务”
   - 在“任务”点击“开始原子化”，观察日志与进度
   - 点击“一键下载（ZIP）”，解压后检查 `使用说明.txt`、`tasks_atomic.md`

## 6. 关键接口自检结果（本次冒烟）

- `GET http://localhost:3030/health` → `200`（`ok: true`）
- `GET http://localhost:3030/specs` → `200`（`specsCount: 57`）
- `GET http://localhost:3030/prompts` → `200`（`version: 1`）
- `GET http://localhost:3030/llm/ping?model=gpt-5.2-codex` → `200`（`ok: true`，`latencyMs: 5270`）
