# 风险与依赖

## 依赖
- 本机已安装并运行 MySQL 8.0。
- 管理后台可获取数据库连接信息（主机、端口、账号、密码）。

## 风险
- **数据体量增长**：LLM 对话原文全量保存，长期增长会带来存储压力。
- **性能影响**：Bridge 写入路径增加落库逻辑，需避免影响主流程响应。
- **敏感信息暴露**：完整对话原文可能包含敏感内容，需确保后台访问受控。
- **一致性风险**：多阶段写入时机不一致，可能导致 specId 关联不完整。
- **单点依赖**：本地 MySQL 不可用将导致数据落库失败，需要明确失败策略与告警。
