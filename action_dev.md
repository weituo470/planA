# 项目进化行动记录（action_dev）

> 说明：本文记录我在本仓库内“可观测到”的系统演进路径（从代码结构、交互能力、稳定性与运维可观测性角度），用于后续 DEBUG、复盘与持续优化。  
> 范围：`bridge/`（任务编排/终端桥接）与 `dashboard/`（可视化编排/执行 UI）。

## 0. 一句话定位
把“需求文档 → 任务拆解（TASKS_JSON）→ DAG 编排 → CLI 执行 → 状态回写/冲突控制 → 可观测日志”串成闭环，并且让问题暴露而不是被兜底掩盖。

## 1. 关键演进主线

### 1.1 从“文档驱动”到“可编排任务”
- 通过 `tasks.md` 的 `## TASKS_JSON` 区块承载结构化任务列表，使任务可以被系统解析、可视化，并作为并发调度的输入。
- 任务对象逐步统一为：
  - `id`（形如 `task_1` 连续编号）
  - `title/description/dependencies/scope`
  - （可选）`estimated_complexity`、`cliToolId`、状态字段（`startedAt/doneAt/status` 等）

### 1.2 从“静态 DAG 展示”到“可交互/可恢复 DAG”
- DAG 组件负责渲染任务节点与依赖边，并在发生渲染异常时给出可感知的异常信号（而不是静默黑屏）。
- 当前已能在异常场景写入测试日志（见后文“可观测性”），并在部分可恢复异常（例如“节点全部在视口外”）触发自动恢复。

### 1.3 从“手动执行”到“可调度 CLI 执行”
- 引入终端面板，统一承载 CLI 运行环境（可多终端、可切换、可记录输出）。
- 任务执行逐步演进为：
  - 在 UI 触发“开始/继续”→ 自动展开终端区域 → 创建对应 CLI 终端 → 写入任务提示词 → 自动回车触发执行（适配 Windows 下 CLI 行为差异）。
  - 为避免“终端已启动但任务仍显示可开始”的不一致，启动时先写回任务状态为 `running`。

### 1.4 从“只看依赖”到“并发冲突可控”
- 在依赖之外引入 `scope（写锁）` 的概念：用于降低并发改同文件导致冲突的概率。
- 在任务调度时把“被依赖阻塞”和“资源占用阻塞”区分展示：同一文件/目录的并发写入会触发资源阻塞提示。

### 1.5 从“不可复盘”到“可追踪测试会话”
- 引入 `testSessionId`（前端生成并持久化）与请求头 `x-test-session-id`。
- Bridge 侧自动记录携带该 header 的所有 HTTP 请求（方法/路径/状态码/耗时/请求体摘要等），并以 JSONL（NDJSON）落盘：
  - `logs/test-sessions/<sessionId>.jsonl`
- Dashboard 侧补齐：
  - 全局 `window.error` / `unhandledrejection` 上报
  - 关键交互（终端创建/输入/暂停/恢复、链路开始/暂停/继续等）上报
  - 任务页内置“测试日志”面板（默认折叠）用于实时查看/过滤/下载日志

## 2. 近期稳定性问题与修复要点

### 2.1 Codex 自动启动失败（exec 卡住）
现象：在“非交互 exec”场景下，Codex CLI 可能因为本机 `~/.codex/config.toml` 配置了 `mcp_servers`（如 `claude-code`、`chrome-devtools`），导致 exec 模式启动 MCP 后进程无法正常退出，从而让调度 Worker 卡住。

处理：对 exec 场景增加显式配置覆盖，强制禁用 MCP：
- `-c mcp_servers.claude-code.enabled=false`
- `-c mcp_servers.chrome-devtools.enabled=false`

目的：保证 exec 任务能按预期退出、回写状态、继续调度后续任务；同时不影响“终端长会话模式”按用户本机配置启用 MCP。

## 3. 约束与原则（贯穿演进）
- 不做兜底：信息不足/执行失败必须显式报错并记录日志，便于定位问题。
- 可复现：关键路径（DAG 渲染异常、CLI 启停、任务状态回写、并发冲突）必须能通过日志回溯到具体会话与时间点。
- 最小化修改：尽量在任务声明的 `scope` 范围内实现；越界修改需要显式暴露原因与影响。

## 4. 后续可选演进方向（备忘）
- 更细粒度的“资源锁”：把 `scope` 从目录级进一步细化到文件级，并引入“读锁/写锁”。
- 执行态与日志联动：点击任务卡片可直达对应 `testSessionId` 日志片段（按 taskId/terminalId 过滤）。
- 对“异常但可恢复”的 UI 行为做更明确提示：例如恢复次数、最近一次恢复原因、用户一键导出诊断包。

