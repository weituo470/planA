# 重构备选计划

## 背景
项目目前存在少数“超大型文件”，已明显影响：
- 阅读/定位成本（单文件承载过多职责）
- 变更冲突概率（多人并行改动同一文件）
- 回归风险（牵一发而动全身）
- 可测试性（难以做针对性单测/集成测）

本文件给出若干可选重构方案（从“小步快跑”到“结构升级”），便于按风险/收益/周期做取舍。

## 现状（重点超大型文件）
仅统计“非产物/非依赖目录”的文件（已排除 `node_modules/`、`dist/`、`.git/` 等）。

| 类别 | 路径（仓库相对） | 规模 | 备注 |
| --- | --- | --- | --- |
| 后端 Bridge | `bridge/src/index.js` | ~9393 行 / ~364 KB | 路由、配置、LLM 调用、Spec/Report、终端等全部集中 |
| Dashboard | `dashboard/src/App.tsx` | ~4672 行 / ~203 KB | 页面状态、API、流程编排、多个面板 UI 逻辑集中 |
| Dashboard | `dashboard/src/TerminalPanel.tsx` | ~1194 行 / ~42.5 KB | 终端 tab、socket、API、UI 逻辑较集中 |
| Dashboard | `dashboard/src/components/mvp5/ManualTaskRunner.tsx` | ~30 KB | 任务提示词/状态/动作、与终端联动 |
| 运行产物 | `bridge/data/events.jsonl` | ~2.3 MB | 事件日志（建议视为运行产物而非源码） |

## 共同目标（不依赖具体方案）
- 把“单文件多职责”拆成：**路由/协议层、领域服务层、存储层、UI 组件层、状态/副作用层**。
- 维持外部行为不变（接口路径、字段、UI 关键交互），以“等价重构”为主。
- 逐步落地：优先可独立合并的小 PR，避免大爆炸式重写。
- 为关键路径补最小回归：`/health`、任务页 DAG、终端自动执行链路。

---

## 方案 A：Dashboard 先拆 `App.tsx`（最小风险，优先级最高）
**核心思路**：把 `App.tsx` 里的“纯函数工具 / API 调用 / 业务状态 / 视图面板”分层拆出，但不引入新依赖、不改变路由/交互。

**建议拆分（示例目录草案）**
- `dashboard/src/api/bridge.ts`：`apiJson`、`apiNdjsonStream`、统一错误处理/超时
- `dashboard/src/lib/download.ts`：`downloadBlob` / `downloadMarkdown` / `basenameFromAnyPath`
- `dashboard/src/lib/text.ts`：`stripAnsi`、文本处理
- `dashboard/src/features/requirements/RequirementsPanel.tsx`
- `dashboard/src/features/design/DesignPanel.tsx`
- `dashboard/src/features/tasks/TasksPanel.tsx`（承接任务页主视图：DAG、列表、流程报告折叠等）
- `dashboard/src/features/prompts/PromptConfigPanel.tsx`
- `dashboard/src/hooks/useSpecs.ts`、`useReports.ts`、`useCliTools.ts`、`useLlm.ts`：把“加载/保存/轮询/副作用”从 UI 中剥离

**落地步骤（可拆 3~5 个 PR）**
1) 先抽离纯工具函数与 API client（零 UI 改动）
2) 抽离“提示词配置”面板为独立组件
3) 抽离“任务页”面板（含 DAG + 任务列表 + 流程报告折叠）
4) 把剩余页面逐步移入 `features/*`，`App.tsx` 收敛为“布局 + 顶层状态编排”

**收益**
- 变更冲突显著下降；阅读定位更快
- 可对 `features/tasks` 做更聚焦的回归与性能优化

**风险**
- 主要是 import/props 迁移错误；可通过构建与关键交互回归快速兜底

**适用场景**
- 需要尽快降低维护成本，同时不想改变整体架构/依赖

---

## 方案 B：Dashboard 结构升级为“Feature First + 代码分割”（收益更高，中等风险）
**核心思路**：在方案 A 的基础上，把任务页/需求页/设计页等改为按 feature 组织，并对大块功能做 `React.lazy` 级别的代码分割，降低首屏 JS 体积、让大功能互不干扰。

**关键点**
- `features/*` 目录内自包含：`components/ hooks/ types/` 就近放置
- 大面板使用 `React.lazy(() => import(...))`，仅在切换到对应 artifact 时加载
- 将跨 feature 的共享能力（toast、api、download、types）留在 `src/lib`、`src/api`、`src/common`

**收益**
- 体感性能更稳（任务页/终端/报告不再把首屏拖大）
- 更清晰的边界与长期扩展性

**风险**
- 动态加载带来的 loading/错误边界需要补齐；需要更完整的 UI 回归

**适用场景**
- Dashboard 功能会继续膨胀，希望把架构一次性“拉直”

---

## 方案 C：Bridge 拆 `bridge/src/index.js` 为“server + routes + controllers + services”（推荐）
**核心思路**：保持现有 HTTP 路径与返回结构不变，把巨型文件拆成多模块，按职责归档，减少“全局共享状态”。

**建议目录草案**
- `bridge/src/server.js`：启动与监听（`PORT`、日志）
- `bridge/src/app.js`：Express app 初始化、中间件、错误处理
- `bridge/src/routes/*.routes.js`：仅做路由注册与参数校验（轻薄）
  - `health.routes.js`、`fs.routes.js`、`terminals.routes.js`、`llm.routes.js`、`prompts.routes.js`
  - `specs.routes.js`、`reports.routes.js`、`mvp5.routes.js`
- `bridge/src/controllers/*.controller.js`：组装请求/响应、调用 services
- `bridge/src/services/*`：保留/复用现有 services（已存在部分）
- `bridge/src/lib/*`：工具函数（nanoid、模板渲染、JSON/NDJSON、错误归一等）
- `bridge/src/store/*`：本地文件持久化（prompts/cli-tools/llm-config/specs/reports）

**落地步骤（可拆 4~8 个 PR）**
1) 先“纯移动”：把工具函数按领域切到 `lib/`（不改逻辑）
2) 抽出 `routes/` 注册层，`index.js` 只做 `app.use(...)`
3) 把 `/api/mvp5/*`、`/specs/*`、`/terminals/*` 分别迁入 controller/service
4) 最后把 `index.js` 收敛到 `server.js + app.js` 两个薄文件

**收益**
- 未来加接口/改逻辑时不会反复触碰同一巨文件
- 更容易做局部测试与压测（例如只测 mvp5 路由）

**风险**
- 路由迁移时漏挂载/顺序变化导致行为差异；可以用最小 smoke tests 覆盖：`/health`、`/specs`、`/prompts`、`/api/mvp5/task-prompt`

**适用场景**
- Bridge 还会继续加能力（终端/LLM/任务编排/报告），需要长期可维护

---

## 方案 D：Bridge 迁移 TypeScript（长期收益高，周期长）
**核心思路**：在方案 C 的模块化边界基础上逐步 TS 化（先 `checkJs` 再逐步 `.ts`），为请求体/响应体/配置结构提供类型约束。

**建议落地路径**
- 第 1 阶段：`tsconfig` + `// @ts-check` + JSDoc typedef（不改文件后缀）
- 第 2 阶段：新增 `types.ts`（共享 DTO），核心模块逐步改为 `.ts`
- 第 3 阶段：为关键请求体做运行时校验（可选：Zod），错误统一格式

**收益**
- 大幅降低“字段拼错/漏判空/隐式 any”带来的线上问题

**风险**
- 初期会引入类型改造成本；对构建/运行脚本要同步调整

**适用场景**
- Bridge 已进入“稳定长期维护期”，并且频繁迭代导致回归成本高

---

## 方案 E：把运行产物/大文件从源码仓库边界移出（立即见效，低风险）
**核心思路**：明确哪些属于“运行产物/日志/历史报告”，不与源码同一生命周期，避免仓库持续膨胀与 merge 冲突。

**建议处理对象**
- `bridge/data/events.jsonl`（事件日志）
- `docs/flow_*.md`（流程报告历史，若仅作运行产物可迁出或归档）

**可选策略**
- 策略 1：移动到 `.runlogs/`（或 `var/`）并纳入 `.gitignore`
- 策略 2：保留少量样例（例如 1~2 份），其余外置/压缩归档
- 策略 3：增加“保留策略”（例如仅保留最近 N 份/最近 N 天）

**收益**
- 仓库体积与噪音下降；`git status`/diff 更清爽

**风险**
- 需要确认这些文件是否被当作“示例/fixture”；若是示例，应改为 `docs/examples/` 并精简体积

---

## 选择建议（按目标倒推）
- **优先降低维护成本、尽量不动架构**：方案 A + 方案 C
- **希望同时改善性能与长期扩展**：方案 B + 方案 C
- **希望彻底提升稳定性与可测试性**：方案 C + 方案 D（周期更长）
- **仓库体积先瘦身、减少噪音**：方案 E（可独立先做）

## 验收口径（建议）
- `dashboard/`：`npm run build` 通过；任务页 DAG、终端自动执行链路关键交互保持一致
- `bridge/`：`/health` 返回 `{"ok":true}`；核心接口（prompts/specs/mvp5）返回结构不变

