# 无人值守任务 scope 越界处理研究

## 1. 问题现象（来自 task_3 运行输出）
在任务编排中，`scope` 被定义为“只允许修改的目录/文件集合”。当某个任务的功能依赖一个“应用级接线点”（例如在 `main.tsx`/`App.tsx` 注入 `StoreProvider`）但该接线点不在当前任务 scope 内时，CLI Agent 会进入“越界询问”流程，要求人工确认：

- 任务无法继续推进（破坏无人值守）
- 即使逻辑上“必须修改”，也会被 prompt 指令拦下
- 该情况本质上是**任务拆解/Scope 设计不完备**与**Scope 语义过于强硬**共同导致的

## 2. 触发根因（在本仓库可定位的具体触发点）
当前 MVP5 的任务执行文档/提示词包含硬性语句：

- “优先只修改本任务 scope 列表内文件/目录；若必须修改 scope 外内容，先停止并说明原因与影响。”

该语句会让 Agent 在遇到“接线缺失但必须接线”的场景时，选择停下来询问，而不是继续完成闭环交付。

## 3. 需求目标（无人值守前提下的正确行为）
在“不做兜底、问题必须暴露”的约束下，目标不是“让 Agent 任意越界”，而是让系统具备**可解释、可审计、可自动化**的越界处理策略：

1) 不出现交互式提问（无人值守不中断）  
2) 越界发生时必须显式记录（日志/回写），可复盘  
3) 并发安全仍然成立（越界文件必须纳入写锁/冲突检测）  
4) 超出可控范围的越界必须失败并暴露原因（而不是沉默继续）

## 4. 解决方案候选（从“最稳妥”到“最自动化”）

### 方案 A（推荐，源头治理）：强制加入“应用接线/集成”任务
在 tasks 生成阶段，**默认生成一个早期的“应用接线（Wiring）”任务**，scope 覆盖：

- `src/main.tsx` / `src/App.tsx`
- `src/providers/**`（或约定的 provider 聚合目录）

并让所有可能引入“全局上下文依赖”的任务（store/router/i18n/theme/di container 等）依赖该 wiring 任务。

优点：
- 最符合“scope 是计划的一部分”的理念
- 不需要执行期越界
- 并发锁更清晰

缺点：
- 任务生成 prompt 必须更聪明（识别哪些能力需要接线）
- 仍可能出现后续新增全局依赖（例如中途新增 Provider）

### 方案 B（推荐，执行期自动化）：预授权“接线类越界白名单”
把 scope 的角色从“强权限边界”调整为“并发写锁 + 默认修改范围”，并引入“接线白名单”，当且仅当满足以下条件时允许自动越界：

- 越界文件属于白名单（例如 `src/main.tsx`、`src/App.tsx`、`src/providers/**`）
- 越界目的属于接线（例如注入 Provider、Router 包裹、全局 CSS/Theme 注入）
- 修改必须是最小 diff（只加一层 wrapper，不做大规模重构）

同时要求：
- 自动将越界文件追加进该任务的 `scope（写锁）`（或在系统侧维护“运行期写锁扩展”）
- 写入明确的日志事件：`scope_exception`（原因、文件列表、摘要 diff）

优点：
- 不打断无人值守
- 仍然可审计、可复盘
- 对并发冲突可控（写锁被扩展）

缺点：
- 需要定义白名单与“接线判定规则”
- 需要系统/提示词层面落实“扩展 scope + 记录”机制

### 方案 C（更强自动化）：自动生成“补丁任务”而不是越界
当任务发现必须修改 scope 外文件时，不继续提问，而是：

1) 记录一条结构化事件（需要修改的文件、原因、最小建议变更）
2) 由编排器自动生成一个新的任务（例如 `task_wiring_fix_x`），scope 覆盖接线点
3) 将该补丁任务插入 DAG（成为当前任务的依赖），然后继续无人值守调度

优点：
- 保持“每个任务只改自己 scope”的原则
- 任务审计更清晰（越界不会发生，只会生成新任务）

缺点：
- DAG 在运行期发生结构变更，需要更复杂的状态与回写策略
- 需要 UI 解释“为什么新增了任务”

### 方案 D（工程化补强）：执行前的“接线预检”
在开始执行 DAG 前做一次静态/半静态预检，例如：

- 若某任务 scope 内引入了 `useXxx()` 且 `useXxx` 明确依赖 Provider，则检查 `main.tsx/App.tsx` 是否已接线
- 若未接线，则在执行前自动插入 wiring 任务或扩展 scope

优点：
- 提前发现问题，避免运行中断

缺点：
- 需要一定的代码分析/约定（Provider/Hook 命名、目录结构）

## 5. 对本仓库的落地建议（面向 MVP5）
结合当前已有的“scope（写锁）/并发冲突控制/测试会话日志”能力，推荐分两步落地：

### 第一阶段（低风险、立刻减少中断）
把现有 prompt 中的“越界先停止并说明原因与影响”调整为“按策略处理”，默认策略为：

- 若越界仅涉及接线白名单（`src/main.tsx` / `src/App.tsx` / `src/providers/**`），允许继续并强制记录 `scope_exception`
- 若越界超出白名单：直接失败并输出结构化错误（不中断等待人工输入）

这不会引入“兜底”，因为：
- 失败仍然显式暴露
- 允许的越界是显式策略 + 记录 + 可审计

### 第二阶段（源头治理，提升任务质量）
在 tasks 生成 prompt 中加入强制规则：

- 任何会引入 Provider/Context 的任务，要么把接线点纳入同一任务 scope，要么显式生成 wiring 任务并写 dependencies
- “scope（写锁）”必须覆盖“为了验收而必须修改的文件”，否则任务不合格

## 6. Opus 4.5 讨论（当前通道状态）
本次尝试通过 Bridge 的 `claude-opus-4-5-20251101` 模型做讨论，但当前配置的 anthropic 网关返回“没有可用token”，导致无法完成对话。

在需要启用 Opus 4.5 讨论时，建议把“模型可用性”作为前置健康检查项（例如 ping 成功后再进入对话/生成阶段），避免把不可用问题混入任务质量问题。

