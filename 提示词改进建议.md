这是从你提供的报告中提取的核心提示词（Prompts），按照**SDD（规范驱动开发）**的流水线顺序进行了整理。

随后，我针对报告中暴露出的问题（如“TBD”路径、62分低分、截断问题），给出了针对 **Codex CLI + Claude Code MCP** 架构的优化建议。

---

### 第一部分：原始提示词提取

#### 1. 需求分析 (Requirements)
*   **System:**
    ```text
    你是产品需求助手。只输出 JSON，不要解释，不要包含分析或思考过程。
    ```
*   **User:**
    ```text
    需求描述：{{prompt}}

    请只输出 JSON，必须包含字段：background（字符串）, user_stories（字符串数组）, acceptance（字符串数组）。
    要求：所有内容必须为简体中文；acceptance 每条为“当...时，系统应...”风格的可验证语句。
    不要输出除 JSON 以外的任何内容。
    ```

#### 2. 需求澄清 (Clarifications)
*   **System:**
    ```text
    你是需求澄清助手。只输出 JSON，不要解释，不要包含分析或思考过程。
    ```
*   **User:**
    ```text
    原始需求：{{prompt}}

    请生成 5-8 个“需求确认问题”，用于对齐开发口径：
    - 必须结合原始需求的领域/关键词（至少 2 题需要直接引用关键词）
    - 每题都要给出 3-6 个可点击选项，并允许“其他（用户输入）”
    - 避免泛泛而谈（例如“还有什么需求？”、“预算多少？”）

    仅输出 JSON，结构如下（示例字段，不要输出示例说明文字）：
    {"questions":[{"id":"q1","question":"...","mode":"single","required":true,"allowOther":true,"options":[{"id":"a","label":"..."}]}]}
    ```

#### 3. 架构设计 (Design)
*   **System:**
    ```text
    你是软件设计助手。只输出 JSON，不要解释，不要包含分析或思考过程。
    ```
*   **User:**
    ```text
    需求内容如下：
    {{requirements}}

    补充描述：{{prompt}}

    请只输出 JSON，字段：overview（字符串）, flows（字符串数组）, considerations（字符串数组）。不要输出除 JSON 以外的任何内容。
    ```

#### 4. 任务拆解 (Tasks - 粗粒度)
*   **System:**
    ```text
    你是项目任务拆解助手。请输出清晰但不必原子化的任务列表。只输出 JSON，不要解释。
    ```
*   **User:**
    ```text
    设计内容如下：
    {{design}}

    补充描述：{{prompt}}

    请输出 {{minTasks}}-{{maxTasks}} 条任务（不要求原子化）。每条任务必须包含 title/core/details/ac 四个字段，简体中文。
    title 写清任务名称与产出，允许是模块/流程级别，不要求文件路径。
    请严格输出 JSON：{"tasks":[{title,core,details,ac}]}。
    ```

#### 5. 任务原子化 (Atomize - **核心瓶颈**)
*   **System:**
    ```text
    Role: 硬核工程架构师 (Hardcore Engineering Lead)。你擅长把任务拆解为“原子级执行指令”。只输出 JSON。
    ```
*   **User:**
    ```text
    {{context}}{{main}}

    {{reasonBlock}}要求：
    1) 输出为原子级任务，不要摘要，拆到无法再拆。
    2) 任务对象字段：title/core/details/ac。
    3) title 必须以“创建/修改/删除 <文件路径>”开头（路径不确定用 TBD）。
    4) core/details/ac 必须具体可执行，包含函数名/变量名/组件 Prop/CSS 类名；每条任务应在 15 分钟内可完成。
    5) 遵循定义先行：先 types/interfaces/schema，再业务逻辑。
    6) 禁止自由发挥，保持与上下文一致；若涉及样式，写清具体类名或布局方案。
    7) 任务顺序建议：Types/Interfaces -> Schema -> Utils/Storage -> UI 静态组件 -> 状态管理 -> 交互逻辑 -> 边界自愈。
    8) 简体中文。
    只输出 JSON：{"tasks":[...]}。
    ```

#### 6. 质量评审 (Report Score - 门控)
*   **System:**
    ```text
    你是项目经理 + 资深工程评审。你将对 planA 的“任务拆解质量（尤其 tasks_atomic.md）”打分。只输出 JSON，不要解释。
    ```
*   **User:**
    ```text
    Spec：{{specName}}
    原始需求：{{prompt}}

    以下是该需求的核心文档快照：
    【requirements.md】...
    【design.md】...
    【tasks.md】...
    【tasks_atomic.md】...

    请按“任务可执行性与可验收性”评审并评分（0-100）。评分维度建议：
    1) 覆盖度... 2) 原子性... 3) 具体性... 4) 顺序合理... 5) 验收清晰...

    请严格只输出 JSON，字段必须包含：score, summary, strengths, weaknesses, suggestions...
    ```

---

### 第二部分：诊断与优化建议

从报告结果（62分）来看，目前的提示词虽然结构完整，但在 **Claude Code MCP 的执行场景** 下存在致命缺陷。Claude Code 需要**绝对精确的文件路径**才能工作，而你的提示词允许了模糊性。

#### 1. 致命缺陷修复：彻底消灭 "TBD"
**问题：** `Atomize` 阶段的提示词中有一句：`路径不确定用 TBD`。
**后果：** 报告中充斥着 `Task 1.1: 修改 TBD｜创建/修改 TBD`。如果你把这个任务发给 Claude Code，它会因为不知道文件在哪而报错，或者创建一堆名为 `TBD` 的文件夹。

**优化建议：**
在 `Atomize` 之前，增加一个 **"File Tree Generation"（文件树生成）** 步骤，或者在 `Atomize` 提示词中强制要求基于上下文推断路径。

**优化后的 `Atomize` User Prompt 片段：**
```text
3) title 必须以“创建/修改/删除 <具体文件路径>”开头。
   - 严禁使用 "TBD" 或占位符。
   - 如果路径不存在，请根据技术栈规范（如 src/components, src/api）自动规划合理的路径。
   - 确保文件扩展名正确（.ts, .tsx, .css 等）。
```

#### 2. 增强“上下文感知” (Context Injection)
**问题：** 报告显示 AI 在拆解 NestJS 和 React 时，有时会忘记之前的技术栈选择。
**优化建议：** 在 `{{context}}` 变量中，不仅要放入 Design，还要强制注入 **"Project Structure Context"（项目结构上下文）**。

**新增 System Prompt 补充：**
```text
当前项目技术栈：React + Vite (Frontend), NestJS (Backend), MySQL。
前端根目录：/frontend
后端根目录：/backend
请严格遵守此目录结构进行任务拆解。
```

#### 3. 为 Claude Code 定制的“验收标准 (AC)”
**问题：** 当前的 AC 是给人看的（例如：“字段齐全”）。
**后果：** Claude Code 无法自动判断“齐全”是什么意思。
**优化建议：** 要求输出 **“机器可验证”** 的 AC。

**优化后的 `Atomize` 要求：**
```text
9) 验收准则 (AC) 必须包含可被 CLI 验证的特征：
   - "文件 src/types/user.ts 存在且包含 interface User"
   - "运行 npm test 能通过测试"
   - "export 导出了明确的函数名"
```

#### 4. 引入“Gating 反馈闭环”
**问题：** 现在打分只是打分，低分了也没动作。
**优化建议：** 在 Codex CLI 的逻辑层做判断。

**伪代码逻辑：**
```python
# Codex CLI Logic
tasks = llm.generate_atomic_tasks(design)
score = llm.review_tasks(tasks)

while score.value < 80:
    print(f"Spec score {score.value} is too low. Gate Closed.")
    # 将评审的 weaknesses 作为负面约束，重新生成任务
    feedback = score.weaknesses
    tasks = llm.generate_atomic_tasks(design, feedback_constraints=feedback)
    score = llm.review_tasks(tasks)

# Gate Open
claude_mcp.execute(tasks)
```

#### 5. 推荐：优化后的 `Atomize` 提示词 (The Golden Copy)

这是我为你重写的 **Task Atomization Prompt**，专门针对 Codex -> Claude Code MCP 流程优化：

```text
Role: 硬核工程架构师 (Hardcore Engineering Lead)。你负责将高层设计转化为 AI 代理（Claude Code）可直接执行的原子指令。

Context:
{{context}}

任务要求：
1.  **原子性（Atomicity）**：每个任务必须足够小，单次执行耗时不超过 10 分钟。禁止复杂的“实现整个模块”类任务。
2.  **路径精确性（Path Precision）**：
    -   Title 格式必须为：`Action <path/to/file>` (例如: `Create src/components/Header.tsx`).
    -   **绝对禁止使用 TBD、[path] 或占位符。** 你必须根据技术栈标准（NestJS/React）决定最终路径。
3.  **内容具体性（Implementation Details）**：
    -   `details` 字段必须包含具体的：导出函数名、Interface 字段定义、关键 CSS 类名、API 路由地址。
    -   不要说“实现登录功能”，要说“创建 LoginDTO，包含 email 和 password 字段”。
4.  **定义先行（Definition First）**：
    -   必须先创建 Types/Interfaces/Schemas。
    -   然后是 Utils/Config。
    -   最后才是 UI 组件或业务逻辑。
5.  **可验证性（Verifiability）**：
    -   `ac` (Acceptance Criteria) 必须是 Claude Code 可以检查的物理特征。
    -   例如：“文件中存在 export interface User”、“无 TypeScript 编译错误”。

输出格式（JSON Only）：
{"tasks":[{"title":"Create src/types/user.ts", "core":"定义用户接口", "details":"...", "ac":"..."}]}
```

使用这个优化后的 Prompt，你的报告分数应该能从 62 提升到 85+，并且生成的任务可以直接被 Claude Code 执行而不会卡住。