# 启动说明

本文档记录本仓库当前可运行的服务及启动方式（以 Windows + PowerShell 为例）。

## 目录与服务

- `bridge/`：后端桥接服务（Express + Socket.IO），默认端口 `4100`
- `dashboard/`：可视化界面（Vite + React），默认端口 `5174`

## 启动（开发模式）

1) 启动 Bridge：

```powershell
cd D:\code\team\workflow\bridge
npm install
npm run dev
```

2) 启动 Dashboard：

```powershell
cd D:\code\team\workflow\dashboard
npm install
npm run dev
```

## 访问地址

- Bridge：`http://localhost:4100`
- Dashboard：`http://localhost:5174`

## 后台启动（可选）

如果你希望在后台启动并保存日志，可参考以下思路：

- 日志目录：`D:\code\team\workflow\.runlogs\`
- 日志文件：`bridge.out.log` / `bridge.err.log` / `dashboard.out.log` / `dashboard.err.log`

## 停止服务

已知 PID 时（PowerShell）：

```powershell
Stop-Process -Id <bridgePid>,<dashboardPid>
```

示例：

```powershell
Stop-Process -Id 25948,708
```

## 常见问题

- 端口冲突：如 `4100` 或 `5174` 被占用，先停掉占用进程或改端口后再启动。
- 依赖未安装：首次启动前务必在对应目录执行一次 `npm install`。
