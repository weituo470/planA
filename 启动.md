# 启动说明

本文档记录 Action（planA）在「npm 包」与「源码仓库」两种形态下的启动方式（Windows + PowerShell 示例）。

## 方式 A：作为 npm 包使用（无需拉源码，推荐）

启动：

```powershell
npx @weituo470/plana
```

默认值：
- 端口：`4100`
- 项目根目录（RootDir）：`C:\planA`（不存在会自动创建）

配置（任选一种）：
- CLI：`plana config` / `plana config set --root C:\planA --port 4100`
- UI：进入页面后打开「配置中心（端口/默认目录）」折叠区，保存后重启 `plana` 生效

访问：
- UI：`http://localhost:4100`
- Health：`http://localhost:4100/health`

## 方式 B：从源码仓库启动

### B1）同端口托管 UI（接近发布形态）

```powershell
# 先构建 dashboard/dist（只需在 UI 改动后重新执行）
npm --prefix dashboard install
npm --prefix dashboard run build

# 启动（Bridge + 静态 UI，同端口）
node .\bin\plana.js
```

### B2）开发模式（Vite 热更新）

1) 启动 Bridge：

```powershell
cd .\bridge
npm install
npm run dev
```

2) 启动 Dashboard：

```powershell
cd .\dashboard
npm install
$env:VITE_BRIDGE_URL = 'http://localhost:4100'
npm run dev
```

访问：
- Dashboard（开发）：`http://localchost:5174`
- Bridge Health：`http://localhost:4100/health`

## 基础自测（建议）

- Dashboard 构建：在仓库根目录执行 `npm --prefix dashboard run build`，期望成功产出 `dashboard/dist/`
- Bridge 健康检查：访问 `http://localhost:4100/health`，期望返回 `{"ok":true}`
- 配置中心：打开「配置中心（端口/默认目录）」可读取与保存默认端口/默认目录（保存后需重启生效）

## 常见问题

- 端口冲突：如 `4100` / `5174` 被占用，可调整 `plana config set --port <port>`，或在开发模式修改 Vite 端口。
- dashboard/dist 不存在：同端口托管 UI 时需先执行 `npm --prefix dashboard run build`。
