# 启动说明

本文档记录本仓库当前可运行的服务及启动方式（以 Windows + PowerShell 为例）。

## 目录与服务

- `bridge/`：后端桥接服务（Express + Socket.IO），默认端口 `3030`
- `dashboard/`：可视化界面（Vite + React），默认端口 `3031`

## 启动（开发模式）

1) 启动 Bridge：

```powershell
cd D:\demo\planA\bridge
npm install
npm run dev
```

2) 启动 Dashboard：

```powershell
cd D:\demo\planA\dashboard
npm install
npm run dev
```

## 访问地址

- Bridge：`http://localhost:3030`
- Dashboard：`http://localhost:3031`

## 后台启动（可选）

如果你希望在后台启动并保存日志，可参考以下思路：

- 日志目录：`D:\demo\planA\.runlogs\`
- 日志文件：`bridge-3030.out.log` / `bridge-3030.err.log` / `dashboard-3031.out.log` / `dashboard-3031.err.log`

## 停止服务

已知 PID 时（PowerShell）：

```powershell
Stop-Process -Id <bridgePid>,<dashboardPid>
```

示例：

```powershell
Stop-Process -Id 25948,708
```

## 常见问题

- 端口冲突：如 `3030` 或 `3031` 被占用，先停掉占用进程或改端口后再启动。
- 依赖未安装：首次启动前务必在对应目录执行一次 `npm install`。
