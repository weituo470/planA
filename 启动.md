# 启动说明

本文档记录本仓库当前可运行的服务及启动方式（以 Windows + PowerShell 为例）。

## 目录与服务

- `bridge/`：后端桥接服务（Express + Socket.IO），默认端口 `4100`（可用 `WORKFLOW_BRIDGE_PORT` 覆盖）
- `dashboard/`：可视化界面（Vite + React），默认端口 `5174`

## 启动（开发模式）

在仓库根目录（例如：`C:\D\codex\planAv03`）分别启动两个服务。

1) 启动 Bridge：

```powershell
cd .\bridge
npm install
npm run dev
```

2) 启动 Dashboard：

```powershell
cd .\dashboard
npm install
# 可选：指定 Bridge 地址（也可写到 dashboard\.env.local）
$env:VITE_BRIDGE_URL = 'http://localhost:4100'
npm run dev
```

## 访问与健康检查

- Bridge：`http://localhost:4100/health`（期望返回：`{"ok":true}`）
- Dashboard：`http://localhost:5174`（本地默认无需登录）

## 基础自测（建议）

- Dashboard 构建：在 `dashboard/` 执行 `npm run build`，期望成功产出 `dashboard/dist/`
- DAG 交互回归：进入「任务」页，确认节点显示任务序号+名称且不显示任务内容；状态标签可点；“可开始”紫色；可开始卡片的“进行中”按钮为灰色；“进行中”齿轮 2s/圈；“已完成”未完成时灰色、完成后绿色；点击 DAG 卡片“开始”会自动展开终端区域，启动 Claude Code（全自动）终端并自动输入任务提示词 + 回车执行（内部命令为 `claude --permission-mode bypassPermissions`，需本机可执行 `claude`）
- 异常提示：若出现黑屏/空白，会出现「DAG 显示异常」红色条，并在浏览器控制台打印 `[TaskDagGraph] render anomaly`

## 后台启动（可选）

下面示例会把日志写到仓库根目录的 `.runlogs/`（已在 `.gitignore` 忽略）。

```powershell
$repo = "C:\D\codex\planAv03"  # 改成你的仓库路径
$logDir = Join-Path $repo ".runlogs"
New-Item -ItemType Directory -Force -Path $logDir | Out-Null

$bridge = Start-Process -FilePath "npm.cmd" -ArgumentList @("run","dev") -WorkingDirectory (Join-Path $repo "bridge") -PassThru `
  -RedirectStandardOutput (Join-Path $logDir "bridge.out.log") -RedirectStandardError (Join-Path $logDir "bridge.err.log")

$dashboard = Start-Process -FilePath "npm.cmd" -ArgumentList @("run","dev") -WorkingDirectory (Join-Path $repo "dashboard") -PassThru `
  -RedirectStandardOutput (Join-Path $logDir "dashboard.out.log") -RedirectStandardError (Join-Path $logDir "dashboard.err.log")

"bridge pid=$($bridge.Id)"; "dashboard pid=$($dashboard.Id)"
```

## 停止服务

已知 PID 时（PowerShell）：

```powershell
Stop-Process -Id <bridgePid>,<dashboardPid>
```

示例：

```powershell
Stop-Process -Id 25948,708
```

## 常见问题

- 端口冲突：如 `4100` 或 `5174` 被占用，先停掉占用进程，或通过 `WORKFLOW_BRIDGE_PORT` / `npm run dev -- --port <port>` 改端口。
- 依赖未安装：首次启动前务必在对应目录执行一次 `npm install`。
- Dashboard 访问不到 Bridge：检查 `VITE_BRIDGE_URL` 是否指向正确的 Bridge 地址与端口。
